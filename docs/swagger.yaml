info:
  contact: {}
paths:
  /index:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      tags:
      - 首页
  /user/list:
    get:
      responses:
        "200":
          description: code", "message"}
          schema:
            type: string
      tags:
      - 获取用户列表
swagger: "2.0"
